<%= simple_form_for @auction do |f| %>
  <%= f.input :title %>
  <%= f.input :details, required: false %>
  <%= f.input :ends_on, :as => :date_picker %>
  <%= f.input :reserve_price %>
  <%= f.submit class: 'btn btn-primary' %>
<% end %>
